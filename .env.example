# HelloComp Dashboard — Environment Variables
# =============================================

# Public API base URL (accessible from client-side)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Use mock data instead of real API (set to "false" to use live endpoints)
NEXT_PUBLIC_USE_MOCKS=true

# Google Gemini API key for content automation (tools/content-automation)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Shoptet XML Feed Configuration
# ================================
# XML feed URL from your Shoptet e-shop (hellocomp.cz)
# Format: https://hellocomp.cz/action/ExportProducts
SHOPTET_XML_URL=https://hellocomp.cz/action/ExportProducts

# Shoptet API token (optional, for authenticated feeds)
SHOPTET_API_TOKEN=

# Adobe Express Embed SDK (client-side, runs in browser)
# ======================================================
# Client ID from Adobe Developer Console (public — secured by domain allowlist)
# Get yours at: https://developer.adobe.com/console → Adobe Express Embed SDK
NEXT_PUBLIC_ADOBE_EXPRESS_CLIENT_ID=

# App name (must match your Adobe Console project name)
NEXT_PUBLIC_ADOBE_EXPRESS_APP_NAME=HelloComp Photo Factory

# Adobe Background Removal — Server-side proxy (alternative)
# ==========================================================
# Enable/disable endpoint /api/image/remove-background
ADOBE_BG_REMOVE_ENABLED=false

# Adobe endpoint URL for background removal operation
# Example only (replace with your actual Adobe endpoint):
# ADOBE_BG_REMOVE_API_URL=https://image.adobe.io/sensei/cutout
ADOBE_BG_REMOVE_API_URL=

# API key for Adobe endpoint (keep server-only, never expose to browser)
ADOBE_API_KEY=

# Header name used for API key (depends on Adobe product)
ADOBE_API_KEY_HEADER=x-api-key

# Multipart field names (override only if Adobe API requires different keys)
ADOBE_BG_REMOVE_IMAGE_FIELD=image
ADOBE_BG_REMOVE_OUTPUT_FORMAT_FIELD=output_format

# Optional defaults
ADOBE_BG_REMOVE_OUTPUT_FORMAT=png
ADOBE_BG_REMOVE_TIMEOUT_MS=30000
ADOBE_BG_REMOVE_MAX_INPUT_BYTES=15000000

# Security hardening
# Comma-separated list of allowed image hosts for background removal
# Example: cdn.myshoptet.com, *.hellocomp.cz
ADOBE_BG_REMOVE_ALLOWED_HOSTS=

# Simple rate limit (per IP)
ADOBE_BG_REMOVE_RATE_LIMIT_WINDOW_SEC=60
ADOBE_BG_REMOVE_RATE_LIMIT_MAX=30

# Adobe Webhook (async background removal)
# =========================================
# Shared secret token for validating incoming Adobe callbacks.
# Generate a strong random value, e.g.: openssl rand -hex 32
ADOBE_WEBHOOK_SECRET=

# Full publicly reachable URL where Adobe should POST completed results.
# This is passed as the callback URL when triggering async removal.
# Example: https://hellocomp.cz/api/image/webhook/callback
ADOBE_WEBHOOK_CALLBACK_URL=
